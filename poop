from icrawler.builtin import GoogleImageCrawler

google_crawler = GoogleImageCrawler(storage={'root_dir': 'C:/путь'})

print('Почём парсим?')
quabtity = int(input())

print('Чё парсим?')
name = str(input())

google_crawler.crawl(keyword=name, max_num=quabtity)
